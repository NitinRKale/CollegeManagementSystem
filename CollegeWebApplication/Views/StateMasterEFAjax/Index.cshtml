@model IEnumerable<CollegeWebApplication.Models.StateMaster>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="card card-success">
    <div class="card-header">
        <h3 class="card-title">State List</h3>
        <div class="card-tools">
            <button type="button" class="btn btn-tool" data-card-widget="collapse">
                <i class="fas fa-minus"></i>
            </button>
            <button type="button" class="btn btn-tool" data-card-widget="remove">
                <i class="fas fa-times"></i>
            </button>
        </div>
    </div>
    <!-- /.card-header -->
    <div class="card-body">
        <p>
            <a asp-action="Create" class="btn btn-primary">Create New</a>
        </p>

        <div class="table-responsive">
            <table id="jqtblStateList" class="table table-bordered table-hover" style="width:100%">
                <thead>
                    <tr>
                        <th>State Id</th>
                        <th>State Name</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                </tbody>
            </table>
        </div>
    </div>
</div>

@section Scripts {

    <style>
        /* prevent wrapping so horizontal scrollbar appears instead */
        #jqtblStateList th,
        #jqtblStateList td {
            white-space: nowrap;
        }

        /* ensure datatable wrapper uses full width */
        .dataTables_wrapper {
            width: 100%;
        }
    </style>

    <script>
        $(document).ready(function () {
            $('#jqtblStateList').DataTable({
                "ajax": {
                    "url": "/StateMasterEFAjax/GetStateList",
                    "type": "GET",
                    "datatype": "json"
                },
                "columns": [
                    { "data": "StateId" },
                    { "data": "StateName" },
                    {
                        "data": "StateId",
                        "render": function (data, type, row) {
                            return `
                                <a href="/StateMasterEFAjax/Edit/${data}" class="btn btn-primary btn-sm">Edit</a>
                                <a href="/StateMasterEFAjax/Details/${data}" class="btn btn-info btn-sm">Details</a>
                                <a href="/StateMasterEFAjax/Delete/${data}" class="btn btn-danger btn-sm">Delete</a>
                            `;
                        },
                        "paging": true,
                        "lengthChange": false,
                        "searching": true,
                        "ordering": true,
                        "info": true,
                        "autoWidth": false,
                        "responsive": false,  // turn off responsive column collapsing
                        "scrollX": true       // enable horizontal scrolling
                    }
                ]
            });
        });
    </script>
}
